package 

import (
	"ProxyMaster_v2/internal/config"
	"ProxyMaster_v2/internal/infrastructure/remnawave"
	"ProxyMaster_v2/pkg/logger"
	"context"
	"fmt"
	"log"

	"github.com/joho/godotenv"
)

func main() {
	err := godotenv.Load()
	if err != nil {
		log.Fatal(err)
	}

	cfg, err := config.New()

	loggerClient, err := logger.NewZap(cfg.LoggerLevel)
	if err != nil {
		log.Fatal("ошибка инициализации логгера: %w", err)
	}

	remnawaveLogger := loggerClient.Named("remnawave")

	remnaClient := remnawave.NewRemnaClient(cfg, remnawaveLogger)

	// //получене uuid юзера
	// uuid, err := remnaClient.GetUUIDByUsername("admin")
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// //получение инфы о юзере//
	// _, err = remnaClient.GetUserInfo(uuid)
	// //получение статуса юзера
	// status, err := remnaClient.GetUserStatus(uuid)

	err = remnaClient.AddInternalSquad(context.Background(), "873925520", []string{"39a5d513-8b40-46c6-952e-b0b00a19889a"})
	if err != nil {
		log.Println("пиздец")
	}

	user, err := remnaClient.GetUserInfo("969b9c51-1fb9-4ad8-b7ec-ecbb7363d9dc")

	if err != nil {
		log.Println("нету пользователя")
	}

	fmt.Println(user.Response.ActiveInternalSquads)
}
